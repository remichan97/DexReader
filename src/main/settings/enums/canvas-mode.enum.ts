export enum CanvasMode {
  Blurred = 'blurred', // Blurred version of the cover art
  Gradient = 'gradient', // Cover art that gradually fades to nothing from left to right
  Accent = 'accent', // Dominant colour from the cover art
  None = 'none' // No canvas, keep the default solid background
}

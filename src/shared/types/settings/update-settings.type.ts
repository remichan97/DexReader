export interface UpdateSettings {
  autoCheck: boolean // Whether to automatically check for updates on app launch
  autoDownload: boolean // Whether to automatically download updates when found (only applies if autoCheck is true)
}
